# 安全策略

TT16 是本地优先的静态内容产品，没有账户、支付、订单或应用数据库。正式答案与快速猜型轨迹都只保存在当前设备。我们仍把答案/结果/候选权重泄露、跨站脚本、构建链污染、秘密提交和未经同意的统计请求视为高优先级安全问题。

## 支持范围

| 范围 | 安全更新 |
| --- | --- |
| `main` 与当前 GitHub Pages 镜像 | 支持 |
| 仓库中的 Web、小程序和共享核心 | 支持 |
| 已部署后的旧沙盒退休入口 | 支持 |
| Fork、修改版或第三方部署 | 由各自维护者负责 |
| Git 历史中的已删除商业实现 | 不作为活动产品支持；若仍可从官方主机访问，请私下报告 |

## 私密报告漏洞

使用 GitHub 仓库 **Security** 页面中的 Private vulnerability reporting / Report a vulnerability。不要创建公开 Issue、Discussion 或 Pull Request 披露未修复漏洞。

报告请包含：

- 受影响的页面、构建、文件或提交；
- 风险与可能影响；
- 最小、无破坏性的复现步骤；
- 是否涉及答案、人格结果、统计同意、广告边界、部署秘密或供应链；
- 建议修复方式（如有）。

不要提交真实用户数据、AppID、广告位 ID、服务器地址、SSH 密钥、统计凭证或完整日志。必要时先脱敏，并询问维护者如何安全传输额外证据。

## 重点安全不变量

- 答案、进度、评分和报告只在当前设备处理；
- 结果与答案不进入 URL、分享链接、统计、广告或日志；
- 统计需要明确同意，并尊重 DNT 与本地拒绝；
- 广告默认关闭，不能覆盖或解锁内容；
- Web 构建不包含业务 API，小程序不包含真实 AppID、广告位 ID 或秘密；
- 旧沙盒所有 `/api/*` 返回 410，退休 Worker 不绑定或访问 D1；
- 高危依赖审计必须为零，锁文件和 CI 使用 `npm ci`。

## 请勿执行

- 不要对公开站点做 DoS、流量压测或大规模自动化扫描；
- 不要诱导其他用户暴露站点数据、浏览器存储或开发者工具信息；
- 不要尝试访问、修改或删除保留的历史 D1 资源；
- 不要攻击 GitHub、微信、云服务商、GoatCounter 或其他第三方服务；
- 不要公开传播漏洞或访问超出最小证明范围的数据。

## 响应目标

这是小型开源项目，不承诺 24 小时响应。维护者会尽量在 7 天内确认报告，并在确认影响后给出修复或披露计划。在双方协调公开披露前，请保持报告私密。
