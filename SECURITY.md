# 安全策略

TT16 处理匿名答题、订单、权益和报告访问，因此即使当前只使用 Mock 支付，也把结果泄露、授权绕过和凭证处理视为高优先级安全问题。

## 支持范围

| 版本 | 安全更新 |
| --- | --- |
| `main` | 支持 |
| 当前 Cloudflare 沙盒 | 支持 |
| 旧版静态 Demo | 仅处理严重的内容或供应链风险 |
| Fork、修改版或第三方部署 | 由各自维护者负责 |

## 私密报告漏洞

请使用 GitHub 仓库 **Security** 页面中的私密漏洞报告功能（Private vulnerability reporting / Report a vulnerability）。不要创建公开 Issue、Discussion 或 Pull Request 来披露尚未修复的漏洞。

报告请包含：

- 受影响页面、接口或提交；
- 风险和可能影响；
- 最小、无破坏性的复现步骤；
- 是否涉及结果提前泄露、越权、凭证、订单、退款或 token；
- 建议修复方式（如有）。

不要提交真实用户数据、完整恢复凭证、报告 token、Cloudflare 密钥或支付资料。必要时对样例进行脱敏，并先询问维护者如何安全传输额外证据。

## 请勿执行

- 不要对公共沙盒进行流量压测、DoS、自动化撞库或速率限制绕过；
- 不要读取、修改或删除不属于你的会话、订单、工单或报告；
- 不要上传恶意文件、探测 Cloudflare 账户或攻击第三方依赖服务；
- 不要利用漏洞获利、公开传播或访问超出最小证明范围的数据。

## 响应目标

这是一个小型开源项目，不承诺 24 小时响应。维护者会尽量在 7 天内确认收到报告，并在确认影响后给出修复或披露计划。复杂问题可能需要更长时间；在双方协调公开披露前，请保持报告私密。

## 重点安全不变量

- 付费前不能下发人格代码、称号、维度、报告正文或结果专属资源；
- 评分、价格、订单、权益和退款由 Worker 决定；
- 恢复凭证和报告 token 使用高熵随机值，服务端只存哈希；
- 恢复失败采用防枚举响应；
- 退款完成后撤销权益和所有活动报告 token；
- 密钥只能保存在 Cloudflare Secrets 等专用秘密存储中。
