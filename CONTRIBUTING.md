# 参与 TT16

感谢你愿意参与 TT16。我们欢迎工程、内容、设计、研究、测试、可访问性和文档贡献，也希望每次改动都能守住“描述决策偏好，不评判投资能力”的边界。

## 开始之前

请先阅读：

- [README.md](README.md)：项目目标、体验、架构和本地运行；
- [AGENTS.md](AGENTS.md)：代码与自动化贡献必须遵守的产品、安全和部署不变量；
- [CODE_OF_CONDUCT.md](CODE_OF_CONDUCT.md)：社区协作约定；
- [SECURITY.md](SECURITY.md)：漏洞报告方式。

如果改动较大，建议先创建 Issue，说明问题、目标用户、拟议方案和验收方式，避免实现方向已经偏离项目边界后才进入评审。

## 可参与的方向

- 前端体验：响应式、可访问性、动效、性能、分享卡和报告阅读；
- Worker / D1：接口、安全、幂等、数据生命周期、监控和测试；
- 题目与内容：中性措辞、偏差检查、内容效度、报告行动守则；
- 视觉资产：16 型人格插画、开源宣传素材和设计系统；
- 工程质量：测试、CI、依赖、安全扫描和文档；
- 用户研究：测试协议、匿名反馈方法和不夸大结论的分析。

内容贡献不得包含荐股、收益暗示、具体仓位建议、投资适当性结论或心理诊断。

## 本地开发

需要 Node.js 22+、npm 和一个可运行 Wrangler 的环境。

```bash
git clone https://github.com/wzxsph/TT16.git
cd TT16
npm ci
npm run db:migrate:local
npm run dev:commercial
```

默认地址为 `http://127.0.0.1:8787`。本地使用 Mock 支付和本地 D1，不需要真实支付账户。

## 建议工作流

1. Fork 仓库并从最新 `main` 创建短生命周期分支；
2. 分支名使用 `feat/…`、`fix/…`、`docs/…`、`test/…` 或 `design/…`；
3. 只修改与当前问题有关的文件，保留工作区中的其他改动；
4. 为行为变化补充测试，为 UI 变化附上截图或录屏；
5. 运行与风险相称的质量检查；
6. 创建 Pull Request，说明问题、方案、测试证据、风险和回滚方式。

提交信息应简短且使用祈使语气，例如：

```text
Add keyboard navigation to the quiz
Prevent report fields from leaking before payment
Clarify Mock checkout in README
```

## 质量检查

应用代码的完整质量门：

```bash
npm run build
npm run typecheck:worker
npm test -- --run
npm run test:api:commercial
npm audit --audit-level=high
```

仅修改文档时，至少运行：

```bash
git diff --check
```

并人工确认 Markdown 链接、图片路径、命令和在线演示地址有效。

## Pull Request 检查单

- [ ] PR 只解决一个清晰问题，没有混入无关格式化；
- [ ] 已说明用户可见变化及其原因；
- [ ] 已列出实际运行的测试命令与结果；
- [ ] 没有提交密钥、token、恢复凭证、个人信息、D1 数据或日志；
- [ ] 付费前的 API 和页面没有泄露人格结果；
- [ ] 评分、价格、订单和权益仍由 Worker 决定；
- [ ] 新文案不构成投资建议、收益承诺或人格优劣排名；
- [ ] 数据库变化使用新的顺序迁移，没有改写已发布迁移；
- [ ] UI 改动在桌面端和约 390px 移动端完成检查；
- [ ] README、配置和测试随行为变化同步更新。

## 数据库与 API 改动

- 在 `migrations/` 增加新的编号 SQL 文件，禁止修改可能已部署的迁移；
- 尽量保持新 Worker 与现有沙盒数据库向后兼容；
- 为错误响应保留防枚举行为，不回显凭证是否存在；
- 金额、SKU、评分、支付成功和权益状态不得由客户端控制；
- 任何新事件字段都必须使用白名单，且不得包含自由文本或秘密。

## 题目与人格内容

提交题目或报告文案时，请同时说明：

- 它想观察的单一行为倾向；
- 左右选项是否在社会赞许度和措辞强度上尽量平衡；
- 是否混入风险能力、投资知识或结果好坏；
- 如何用真实用户认知访谈或匿名测试验证；
- 报告提醒是否可以转化为可观察、可执行的动作。

不要仅凭直觉宣称量表“科学”“准确”或“经过验证”。任何效度主张都需要公开方法、样本边界和证据。

## 视觉资产

- 线上 WebP 放在 `public/images/`，高分辨率母版放在 `design/`；
- README 截图和宣传图放在 `docs/images/`；
- 不要提交来源和授权不明的参考图、字体或第三方品牌素材；
- 截图不得包含订单号、恢复凭证、报告 token、个人信息或账户界面；
- 视觉修改需要检查 alt 文本、对比度、窄屏布局与文本准确性。

## 安全问题

请不要为漏洞创建公开 Issue，也不要在公共沙盒上进行破坏性验证。按 [SECURITY.md](SECURITY.md) 使用私密渠道报告，并提供最小可复现信息。

## 许可证与贡献授权

项目拟采用 `AGPL-3.0-only`，但正式许可证仍等待维护者最终确认。在 `LICENSE` 写入前，仓库不会合并外部代码贡献；许可证确定后，提交 Pull Request 即表示你有权提交该内容，并同意按仓库许可证发布它。
