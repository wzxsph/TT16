# 参与 TT16

感谢你愿意参与 TT16。我们欢迎工程、内容、设计、研究、测试、可访问性和文档贡献，也希望每次改动都守住“描述决策偏好，不评判投资能力”的边界。

## 开始之前

请先阅读 [README.md](README.md)、[AGENTS.md](AGENTS.md)、[CODE_OF_CONDUCT.md](CODE_OF_CONDUCT.md) 和 [SECURITY.md](SECURITY.md)。较大改动建议先创建 Issue，说明用户问题、方案、隐私边界和验收方式。安全问题不要公开讨论。

## 可参与方向

- Web：响应式、可访问性、静态预渲染、性能、分享卡和打印；
- 微信小程序：本地存储、Canvas 海报、分包、开发者工具兼容与发布检查；
- 共享核心：题库、计分、存储迁移、类型对照和平台无关 API；
- 内容：中性措辞、优势/盲点配对、行动守则、指南和传播材料；
- 视觉：低多边形人格插画、Open Graph 图和公开宣传素材；
- 工程质量：Vitest、Playwright、CI、依赖安全、链接和构建扫描。

不接受付费墙、订单、支付、激励广告解锁、券商数据导入、收益预测、荐股、人格能力排名或心理诊断功能。

## 本地开发

需要 Node.js 22+ 和 npm。

```bash
git clone https://github.com/wzxsph/TT16.git
cd TT16
npm ci
npm run dev
```

Web 默认使用 Vite 本地地址。微信小程序执行 `npm run build:weapp` 后，用微信开发者工具打开 `apps/weapp`；只使用仓库中的 `touristappid` 或自己的本地私有配置。

## 建议工作流

1. 从最新 `main` 创建短生命周期分支；
2. 只修改当前问题相关文件，保留工作区其他改动；
3. 为行为变化补测试，为 UI 变化检查桌面与约 390px 宽度；
4. 运行与风险相称的质量门；
5. PR 说明用户变化、隐私影响、测试证据、风险与回滚方式。

## 质量检查

应用改动运行：

```bash
npm ci
npm run quality
```

若只修改文档，至少运行：

```bash
npm run test:links
git diff --check
```

## 内容贡献

题目或报告文案应说明它观察的单一行为倾向，并检查：

- 两端在社会赞许度和措辞强度上是否平衡；
- 是否混入知识、结果好坏、风险能力或财务状况；
- 是否把优势过度使用后的风险写成可观察行为；
- 是否给出能在下一次复盘中核对的动作；
- 是否避免“科学准确”“预测收益”等无公开证据的主张。

不得包含具体证券、仓位建议、收益暗示、投资适当性结论或人格优劣排名。

## 数据与隐私

- 新功能默认不收集数据；能在本地完成就不增加网络请求。
- 快速猜型的题目回答、候选权重、猜中/猜错和强化学习轨迹不得进入统计或网络；合成模拟指标不能写成真实准确率。
- 不发送逐题答案、人格结果、维度比例、自由文本或持久访客 ID。
- 匿名统计事件只能来自固定白名单；增加事件必须同时更新隐私页、测试和评审说明。
- 真实 AppID、广告位 ID、服务器 IP、SSH 密钥和后台凭证不得提交；canonical URL 会进入公开 HTML，不得把秘密放入其中。
- 小程序私有配置使用被忽略的 `project.private.config.json`，部署秘密使用 GitHub Environments / Secrets。

## 视觉资产

- WebP 发布图放在 `public/images/`，高分辨率母版放在 `design/`；
- Open Graph 图由 `scripts/generate-free-assets.ts` 从 v2 母版生成；
- 不提交来源或授权不明的参考图、字体和品牌素材；
- `参考图.png` 不得修改、删除、暂存或提交；
- 检查 alt 文本、对比度、窄屏、打印裁切、免责声明与文字准确性。

## Pull Request 检查单

- [ ] 改动范围聚焦，测试与文档已同步；
- [ ] 本地评分仍是唯一评分路径，答案和结果没有进入网络、URL 或日志；
- [ ] 没有订单、支付、付费材料或广告解锁代码；
- [ ] 广告默认关闭且没有进入答题、报告核心或分享卡；
- [ ] 新文案不荐股、不承诺收益、不排名、不诊断；
- [ ] 没有密钥、AppID、广告位 ID、个人信息或私有服务器配置；
- [ ] Web 桌面与 390px、小程序构建和相关质量门已通过。

## 安全与许可证

漏洞按 [SECURITY.md](SECURITY.md) 私下报告。项目采用 [`AGPL-3.0-only`](LICENSE)；提交 PR 表示你有权按该许可证发布贡献。不要提交来源不明或许可证不兼容的内容与资产。
